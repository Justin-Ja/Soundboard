<Window x:Class="Soundboard.Views.KeyBindingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Soundboard.Views"
        mc:Ignorable="d"
        Title="{Binding SoundName, StringFormat='Set Key Binding for {0}'}"
        Width="350" 
        Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown"
        Focusable="True">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Press any key combination to bind to this sound:"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="1"
                 Text="{Binding KeyDisplayText, Mode=TwoWay}"
                 IsReadOnly="True"
                 Height="30"
                 FontWeight="Bold"
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">

            <Button Content="Clear Binding" 
                    Width="100" 
                    Height="25" 
                    Margin="0,0,5,0"
                    Command="{Binding ClearBindingCommand}"/>

            <Button Content="Cancel" 
                    Width="75" 
                    Height="25" 
                    Margin="0,0,5,0"
                    Command="{Binding CancelCommand}"/>

            <Button Content="OK" 
                    Width="75" 
                    Height="25"
                    IsEnabled="{Binding IsOkEnabled}"
                    Command="{Binding OkCommand}"/>
        </StackPanel>
    </Grid>
</Window>
