using System.IO;
using System.Windows.Input;
using Soundboard.Common;

namespace Soundboard;

public class MainViewModel : BaseViewModel
{
    //When more components are added to the main view and not the button grid, this will be used again, or delegate again
    //But for now it remains empty
    public MainViewModel()
    {
    }
}


